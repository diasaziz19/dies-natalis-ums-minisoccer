// prisma/schema.prisma
// Database Schema - Dies Natalis UMS 2026 Minisoccer Tournament (16-Team Knockout System)

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  SUPER_ADMIN
  COMMITTEE_REFEREE
  TEAM_MANAGER
}

enum TeamStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PlayerPosition {
  GOALKEEPER
  DEFENDER
  MIDFIELDER
  FORWARD
}

enum OfficialRole {
  HEAD_COACH
  OFFICIAL
}

enum MatchStage {
  ROUND_OF_16
  QUARTER_FINAL
  SEMI_FINAL
  THIRD_PLACE
  FINAL
}

enum MatchStatus {
  SCHEDULED
  LIVE
  FINISHED
  POSTPONED
}

enum EventType {
  GOAL
  PENALTY_GOAL
  OWN_GOAL
  YELLOW_CARD
  RED_CARD
  SECOND_YELLOW_RED
  SUBSTITUTION
}

model User {
  id            String    @id @default(uuid())
  name          String
  email         String    @unique
  passwordHash  String
  role          Role      @default(TEAM_MANAGER)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  teams         Team[]    @relation("ManagerTeams")
}

model Team {
  id            String      @id @default(uuid())
  name          String      @unique
  facultyUnit   String      // Fakultas / Unit di UMS
  logoUrl       String?
  managerId     String
  manager       User        @relation("ManagerTeams", fields: [managerId], references: [id])
  managerPhone  String
  status        TeamStatus  @default(PENDING)
  rejectionNote String?
  bracketSeed   Int?        // Seed 1 - 16 saat drawing Knockout
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  players       Player[]
  officials     Official[]
  homeMatches   Match[]     @relation("HomeTeam")
  awayMatches   Match[]     @relation("AwayTeam")
}

model Player {
  id              String         @id @default(uuid())
  teamId          String
  team            Team           @relation(fields: [teamId], references: [id], onDelete: Cascade)
  fullName        String
  identityNumber   String         // NIM (Mahasiswa) / NIDN (Dosen) / KTP
  position        PlayerPosition
  jerseyNumber    Int            // 1 - 99 (Unik per tim)
  photoIdUrl      String?        // Kartu Identitas
  photoProfileUrl String?        // Foto Profil
  isSuspended     Boolean        @default(false)
  suspensionReason String?
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  goals           MatchEvent[]   @relation("GoalScorer")
  assists         MatchEvent[]   @relation("GoalAssister")
  cards           Card[]

  @@unique([teamId, jerseyNumber])
}

model Official {
  id              String       @id @default(uuid())
  teamId          String
  team            Team         @relation(fields: [teamId], references: [id], onDelete: Cascade)
  fullName        String
  identityNumber   String
  role            OfficialRole // HEAD_COACH / OFFICIAL
  photoProfileUrl String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
}

model Match {
  id            String      @id @default(uuid())
  stage         MatchStage  @default(ROUND_OF_16)
  matchNumber   Int         // 1 - 16
  nextMatchId   String?     // Match ID pemenang melaju ke mana
  homeTeamId    String?
  homeTeam      Team?       @relation("HomeTeam", fields: [homeTeamId], references: [id])
  awayTeamId    String?
  awayTeam      Team?       @relation("AwayTeam", fields: [awayTeamId], references: [id])
  homeScore     Int         @default(0)
  awayScore     Int         @default(0)
  penaltyHome   Int?        @default(0) // Adus Penalti jika seri di waktu normal Knockout
  penaltyAway   Int?        @default(0)
  pitchLocation String      // e.g. "Lapangan A UMS Stadium"
  kickoffTime   DateTime
  status        MatchStatus @default(SCHEDULED)
  refereeNotes  String?
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  events        MatchEvent[]
  cards         Card[]
}

model MatchEvent {
  id            String    @id @default(uuid())
  matchId       String
  match         Match     @relation(fields: [matchId], references: [id], onDelete: Cascade)
  minute        Int
  eventType     EventType
  teamId        String
  playerId      String?
  player        Player?   @relation("GoalScorer", fields: [playerId], references: [id])
  assistId      String?
  assister      Player?   @relation("GoalAssister", fields: [assistId], references: [id])
  notes         String?
  createdAt     DateTime  @default(now())
}

model Card {
  id             String    @id @default(uuid())
  matchId        String
  match          Match     @relation(fields: [matchId], references: [id], onDelete: Cascade)
  teamId         String
  playerId       String
  player         Player    @relation(fields: [playerId], references: [id], onDelete: Cascade)
  minute         Int
  isRedCard      Boolean   @default(false)
  isSecondYellow Boolean   @default(false)
  createdAt      DateTime  @default(now())
}
